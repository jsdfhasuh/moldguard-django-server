# MoldGuard 比赛平台必搭流程总清单

## 固定信息

- 后端基础地址：`https://moldguard.oracle.19970219.xyz`
- API 前缀：`https://moldguard.oracle.19970219.xyz/api/v1`
- 平台知识库名称：`模具保养知识库`
- 知识目录版本：`MOLDGUARD-KB-1.2`
- 必搭流程：4 个
- 当前安全配置节点总数：64 个
- 可选知识问答流程不计入必搭范围
- 临时 Webhook 往返测试不计入必搭范围
- 自定义节点源码目录：`../比赛平台_自定义节点/`

员工报工入口只保留 Django 邮件链接页面。平台聊天输入不接收员工图片，也不再搭建“平台页面报工”或分开的“正常/异常报工”流程。

## 必搭清单

| 顺序 | 平台流程名称 | 节点数 | 用途 | 何时搭建 |
|---:|---|---:|---|---|
| 1 | `MoldGuard_00_平台能力探测` | 16 | 验证 GET、POST、动态变量、嵌套 JSON、数组回传和能力报告 | 第一张搭 |
| 2 | `MoldGuard_01_预警派工与知识随单_V2` | 31 | 单输出扫描建单、派工、知识检索与回写、Django 发信、任务卡预览 | 第二张搭 |
| 3 | `MoldGuard_02_Django报工AI审核_V2` | 当前 12 | 接收 Django Webhook、拉取报工材料、执行安全门禁并回写审核建议 | 第三张搭 |
| 4 | `MoldGuard_04_定时器心跳探测` | 最终 5 | 验证平台定时调用能力 P12；保留既有名称避免破坏已保存流程 | 最后搭 |

## 独立文件

1. [01_流程00_平台能力探测.md](./01_流程00_平台能力探测.md)
2. [02_流程01_预警派工与知识随单.md](./02_流程01_预警派工与知识随单.md)
3. [03_流程02_Django报工AI审核.md](./03_流程02_Django报工AI审核.md)
4. [05_流程04_定时器心跳探测.md](./05_流程04_定时器心跳探测.md)
5. [06_临时_Webhook往返测试.md](./06_临时_Webhook往返测试.md)（联调时临时使用）

## 推荐实际搭建顺序

1. 确认 `GET /api/v1/health` 和 `GET /api/v1/meta` 正常。
2. 在自定义组件入口依次粘贴、验证并保存四个 MoldGuard 单输出节点；请求/响应信封显示为 V3，HTTP/知识快照信封显示为 V2。
3. 搭流程 00，保存返回的 `run_id`；本流程不得换成自定义适配器。
4. 在平台创建并选择知识库 `模具保养知识库`。
5. 按 V2 文档新建流程 01，验证扫描自动建单、派工、知识检索与回写、Django 发信和任务卡预览。
6. 把流程 02 的 Webhook endpoint 配置到 Django 的 `MOLDGUARD_REPORT_REVIEW_WEBHOOK_URL`。
7. 需要先排除网络问题时，搭临时 Webhook 往返测试并验证 `roundtrip_status=COMPLETED`。
8. 当前先搭流程 02 的 12 节点 V2 安全版，确认它只能回写 `NEEDS_MORE_INFO`。
9. 员工通过邮件中的 Django 链接提交文字和现场图片，验证平台被唤醒并能拉取完整审核上下文。
10. 只有找到并实测支持图片的多模态节点后，才扩展流程 02 的视觉审核分支。
11. 搭流程 04 的两节点观察版，确认定时器真实 Data 后再扩成五节点心跳版。

## 单输出自定义节点

| 节点 | 职责 | 禁止事项 |
|---|---|---|
| `MoldGuard 请求信封 V3` | 生成单个 `method/url/json_body/context` 请求信封；对未验证视觉审核实施硬门禁 | 不发送 HTTP；不接收平台报工 |
| `MoldGuard 单输入 HTTP V2` | 执行请求信封并原样传递 `context` | 不执行派工、知识或邮件业务规则 |
| `MoldGuard 响应信封 V3` | 提取业务变量，生成带 `[MOLDGUARD_OK]` 或 `[MOLDGUARD_FAIL]` 的单一 Message | 不修改 Django 业务数据 |
| `MoldGuard 知识快照信封 V2` | 把平台知识库结果转换为单个受控知识回写请求 | 不根据文本猜字段 |

流程 01/02 的业务分支由平台原生“如果-否则”节点执行。Webhook、知识库、大模型和聊天输出仍保留在画布上；流程 00 继续使用平台原生 HTTP 节点。

## 旧版兼容节点

以下三个旧版组件只为已存在的流程 04 和回退保留，不再用于新建流程 01/02：

```text
MoldGuard 请求适配器
MoldGuard 响应适配器
MoldGuard 知识快照构建器
```

## 报工权责口径

```text
员工在 Django 页面上传文字和图片
→ Django 保存 ReportSubmission(PENDING_REVIEW)
→ Django Webhook 唤醒流程 02
→ 平台 GET 审核上下文和图片
→ 平台给出 COMPLETE / ABNORMAL / NEEDS_MORE_INFO 建议
→ Django 校验并最终裁决
```

- 平台不得直接写工单状态、周期、履历或告警。
- Webhook 成功只证明平台已被唤醒，不证明报工通过。
- 当前大模型节点未证明能读取图片，所以当前流程只能回写 `NEEDS_MORE_INFO`。
- `COMPLETE` 或 `ABNORMAL` 必须等多模态能力通过真实图片测试后才能启用。
- Django 是完工、异常、周期复位和履历的唯一权威。

## 流程 01 后端前置条件

1. `POST /api/v1/alerts/scan` 命中时同时创建或复用工单，并返回 `work_order_id`。
2. `POST /api/v1/work-orders/{work_order_id}/auto-assign` 返回扁平字段 `assignee_id`。
3. `GET /api/v1/work-orders/{work_order_id}/knowledge-context` 只返回平台检索上下文。
4. 平台调用 `POST /api/v1/work-orders/{work_order_id}/knowledge` 回写实际检索知识包。
5. `POST /api/v1/work-orders/{work_order_id}/send-email` 只接收 `client_request_id`。
6. 发信成功必须返回 `new_email_status=SENT` 和非空 `email_message_id`。

## 能力口径

- 平台大模型任务卡是展示结果，不是邮件发送证明。
- 邮件发送成功只认 Django 的 `new_email_status=SENT` 和真实 `email_message_id`。
- P04 只证明数组原样传递，不证明 `循环` 能展开 HTTP 嵌套数组。
- 平台无法读取报工图片时，P10/P11 不得标记为完成审核能力。
