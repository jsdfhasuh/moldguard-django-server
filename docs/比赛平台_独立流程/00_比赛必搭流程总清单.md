# MoldGuard 比赛平台流程总清单

## 固定信息

- 后端基础地址：`https://moldguard.oracle.19970219.xyz`
- API 前缀：`https://moldguard.oracle.19970219.xyz/api/v1`
- 平台知识库名称：`模具保养知识库`
- 知识目录版本：`MOLDGUARD-KB-1.2`
- 平台实装核对日期：`2026-08-15`
- 正式流程：4 个
- 正式流程可见节点总数：143 个
- 正式流程连线总数：160 条
- 自定义节点源码目录：`../比赛平台_自定义节点/`

员工报工入口只保留 Django 邮件链接页面。平台聊天输入不接收员工图片，也不再搭建“平台页面报工”或分开的“正常/异常报工”流程。

## 平台实装清单

下表按平台当前画布只读核对结果记录。节点数包含画布上仍可见的调试或遗留节点；连线数为 React Flow 画布当前实际连线数。

| 顺序 | 平台智能体 / 工作流名称 | Flow ID | 可见节点 | 连线 | 用途 |
|---:|---|---|---:|---:|---|
| 1 | `MoldGuard-平台能力测试` | `dc8b6510-f2f3-41ee-8f1e-88af4bc9a762` | 16 | 19 | 验证 GET、POST、动态变量、嵌套 JSON、数组回传和能力报告 |
| 2 | `MoldGuard_报工AI审核预警派工与知识随单` | `d85127b8-cfc4-48a4-bd3f-5c431a8c1882` | 50 | 55 | 人工输入“开始检查”，完成扫描、派工、知识随单和发信 |
| 3 | `MoldGuard_报工AI审核预警派工与知识随单-定时触发` | `33ba2057-9074-45c4-ae39-fcff9a14a859` | 51 | 56 | 用定时触发器替代人工入口，复用流程 01 的完整业务链路 |
| 4 | `MoldGuard_Django报工AI审核`（画布名 `MoldGuard_02_Django报工AI审核`） | `d572e00b-1294-47b9-ad9c-2155dec33998` | 26 | 30 | Webhook 拉取 Django 报工材料，执行多模态审核并回写 Django |

计数说明：

- 平台能力测试当前是 19 条连线，`聊天输入.message -> 11_生成回传JSON.probe_tag` 尚未连接；补线后会变为 20 条。本文总数按当前实装计数。
- 手动版流程 01 的 50 个节点包含大模型后的原生“解析器”和调试“聊天输出”；两者都已连接。
- 定时版流程 01 在手动版基础上移除 1 个聊天输入，增加 1 个定时触发器和 1 个解析器，所以是 51 个节点、56 条连线。
- 流程 02 有 24 个已连接业务节点，以及 2 个当前未连线的旧“模板数据注入”节点；因此画布可见 26 个节点，但业务链路仍是 24 个节点、30 条连线。

## 辅助测试智能体

平台还存在以下两个辅助智能体，不计入正式流程总数：

| 名称 | 用途 |
|---|---|
| `模具保养知识库测试` | 单独验证平台知识库检索结果 |
| `webhook测试` | 临时验证 Webhook 往返 |

## 独立文件

1. [01_流程00_平台能力探测.md](./01_流程00_平台能力探测.md)
2. [02_流程01_预警派工与知识随单.md](./02_流程01_预警派工与知识随单.md)
3. [04_流程01_定时触发版.md](./04_流程01_定时触发版.md)
4. [03_流程02_Django报工AI审核.md](./03_流程02_Django报工AI审核.md)
5. [06_临时_Webhook往返测试.md](./06_临时_Webhook往返测试.md)（联调时临时使用）

## 当前执行关系

```text
人工演示：流程 01 手动版的聊天输入
                 └─> 扫描 / 派工 / 知识检索与整理 / 知识回写 / Django 发信

定时演示：流程 01 定时版的定时触发器
                 └─> 同一套扫描 / 派工 / 知识随单 / 发信链路

员工报工：Django 邮件链接页面
                 └─> Django 保存材料并 Webhook 唤醒流程 02
                 └─> 平台审核建议回写 Django，由 Django 最终裁决
```

旧的独立 `MoldGuard_04_定时器心跳探测` 不在当前正式智能体清单中。定时能力现在由流程 01 定时版直接展示，不再把旧五节点心跳流程列为必搭流程。

## 当前搭建与维护顺序

1. 确认 `GET /api/v1/health` 和 `GET /api/v1/meta` 正常。
2. 确认平台已审核组件库中存在流程 01 使用的四个单输出组件；请求/响应信封显示为 V3，知识快照信封显示为 V4，单输入 HTTP 显示为 V2。
3. 运行 `MoldGuard-平台能力测试`，保存返回的 `run_id` 作为能力证据。
4. 确认平台知识库严格选择 `模具保养知识库`。
5. 先用流程 01 手动版验证扫描、自动派工、知识检索、严格 JSON 整理、知识回写、Django 发信和任务卡预览。
6. 再启用流程 01 定时版；当前为 8 秒测试间隔，演示结束后必须停止，避免持续触发扫描和邮件链路。
7. 把流程 02 的 Webhook endpoint 配置到 Django 的 `MOLDGUARD_REPORT_REVIEW_WEBHOOK_URL`。
8. 流程 02 只使用已审核的 `MoldGuard 豆包多模态 V1` 自定义节点；其余节点均为平台原生节点。
9. 员工通过邮件中的 Django 链接提交 1 至 10 张图片；完整自动审核验收场景至少提交 2 张。
10. 完成“只改变图片像素会改变模型观察或结论”的实测后，才开启 `COMPLETE` / `ABNORMAL` 回写门禁。

## 单输出自定义节点

| 节点 | 职责 | 禁止事项 |
|---|---|---|
| `MoldGuard 请求信封 V3` | 为流程 01 生成单个 `method/url/json_body/context` 请求信封 | 不发送 HTTP；不接收平台报工 |
| `MoldGuard 单输入 HTTP V2` | 执行请求信封并原样传递 `context` | 不执行派工、知识或邮件业务规则 |
| `MoldGuard 响应信封 V3` | 提取业务变量，生成带 `[MOLDGUARD_OK]` 或 `[MOLDGUARD_FAIL]` 的单一 Message | 不修改 Django 业务数据 |
| `MoldGuard 知识快照信封 V4` | 接收严格 JSON 中的 `title/content/source`，自动生成内部字段并转换为单个受控知识回写请求 | 不接受说明性自由文本；不让 AI 生成内部 ID；诊断不输出输入值 |
| `MoldGuard 豆包多模态 V1` | 使用外部提示词和批量图片调用平台原生豆包模型，输出中文 Message 和严格 JSON | 不内置审核规则；不直接写 Django |

已审核自定义组件是受控内容。本轮流程整理只记录平台原生节点、配置和连线，不修改、创建、重命名或重新注册任何自定义组件。

## 旧版兼容节点

以下三个旧版组件只为回退和历史流程备份保留，当前四个正式流程均不使用：

```text
MoldGuard 请求适配器
MoldGuard 响应适配器
MoldGuard 知识快照构建器
```

## 报工权责口径

```text
员工在 Django 页面上传文字和图片
-> Django 保存 ReportSubmission(PENDING_REVIEW)
-> Django Webhook 唤醒流程 02
-> 平台 GET 审核上下文和图片
-> 平台给出 COMPLETE / ABNORMAL / NEEDS_MORE_INFO 建议
-> Django 校验并最终裁决
```

- 平台不得直接写工单状态、周期、履历或告警。
- Webhook 成功只证明平台已被唤醒，不证明报工通过。
- Django 是完工、异常、周期复位和履历的唯一权威。

## 流程 01 后端前置条件

1. `POST /api/v1/alerts/scan` 命中时同时创建或复用工单，并返回 `work_order_id`。
2. `POST /api/v1/work-orders/{work_order_id}/auto-assign` 返回扁平字段 `assignee_id`。
3. `GET /api/v1/work-orders/{work_order_id}/knowledge-context` 只返回平台检索上下文。
4. 平台调用 `POST /api/v1/work-orders/{work_order_id}/knowledge` 回写实际检索知识包。
5. `POST /api/v1/work-orders/{work_order_id}/send-email` 只接收 `client_request_id`。
6. 发信成功必须返回 `new_email_status=SENT` 和非空 `email_message_id`。

## 能力口径

- 平台大模型任务卡是展示结果，不是邮件发送证明。
- 邮件发送成功只认 Django 的 `new_email_status=SENT` 和真实 `email_message_id`。
- P04 只证明数组原样传递，不证明 `循环` 能展开 HTTP 嵌套数组。
- 平台无法读取报工图片时，P10/P11 不得标记为完成审核能力。
